{{/*
  settings.json ConfigMap removed in v2.
  All user-editable configuration is now stored in the SQLite database (pvmss.db).
  The database is persisted via the pvmss-db-pvc PersistentVolumeClaim.
  No ConfigMap is needed for application settings.
*/}}
