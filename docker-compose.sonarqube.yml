---
# Local SonarQube Community Build server and one-shot scanner for PVMSS.
# Use with the matching Makefile targets (sonar-up, sonar-scan, sonar-down, ...).

services:
  sonarqube:
    image: sonarqube:26.6.0.123539-community
    container_name: pvmss-sonarqube
    restart: unless-stopped
    ports:
      - "9000:9000"
    environment:
      # Disable Elasticsearch bootstrap checks so the container starts locally
      # without tuning vm.max_map_count on Docker Desktop / OrbStack.
      SONAR_ES_BOOTSTRAP_CHECKS_DISABLE: "true"
    volumes:
      - sonarqube_data:/opt/sonarqube/data
      - sonarqube_logs:/opt/sonarqube/logs
      - sonarqube_extensions:/opt/sonarqube/extensions
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 2G
    networks:
      - sonarqube

  sonar-scanner:
    image: sonarsource/sonar-scanner-cli:12.1.0.3233_8.0.1
    container_name: pvmss-sonar-scanner
    restart: "no"
    environment:
      SONAR_HOST_URL: http://sonarqube:9000
    volumes:
      - .:/usr/src
      - sonar_cache:/opt/sonar-scanner/.sonar
    working_dir: /usr/src
    networks:
      - sonarqube
    depends_on:
      - sonarqube
    profiles:
      - scan

volumes:
  sonarqube_data:
  sonarqube_logs:
  sonarqube_extensions:
  sonar_cache:

networks:
  sonarqube:
    name: pvmss_sonarqube
    driver: bridge
