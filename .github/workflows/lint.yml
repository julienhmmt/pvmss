---
name: Lint

on:
  push: null

permissions: {}

jobs:
    super-linter:
      name: Super Linter
      runs-on: ubuntu-latest
      timeout-minutes: 15

      permissions:
        contents: read
        packages: read
        statuses: write

      steps:
        - name: Checkout code
          uses: actions/checkout@v7
          with:
            # super-linter needs the full git history to get the
            # list of files that changed across commits
            fetch-depth: 0

        - name: Super-linter
          uses: super-linter/super-linter/slim@v8.7.0 # x-release-please-version
          env:
            # To report GitHub Actions status checks
            GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
            DEFAULT_BRANCH: main
            VALIDATE_ALL_CODEBASE: false
            VALIDATE_CSS: true
            VALIDATE_MARKDOWN_PRETTIER: true
            FIX_MARKDOWN_PRETTIER: true
            VALIDATE_YAML: true
            FILTER_REGEX_EXCLUDE: '.*bulma\.min\.css$,.*brands\.min\.css$,.*fontawesome\.min\.css$,.*regular\.min\.css$,.*solid\.min\.css$,.*components/noVNC-1\.6\.0/.*'

    golangci-lint:
      name: Go Lint
      runs-on: ubuntu-latest
      timeout-minutes: 10

      permissions:
        contents: read
        packages: read
        statuses: write

      steps:
        - name: Checkout code
          uses: actions/checkout@v7
          with:
            fetch-depth: 0

        - name: Set up Go
          uses: actions/setup-go@v7
          with:
            go-version-file: server/go.mod

        - name: Run golangci-lint
          uses: golangci/golangci-lint-action@v9
          with:
            version: latest
            working-directory: server
            args: -v --timeout=5m
