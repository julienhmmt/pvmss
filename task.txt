You are working in an isolated git worktree of the PVMSS Go server repo (branch smell-t11), at /Users/jh/git/gh/wt-smell/t11.

TASK: Resolve 1 SonarQube new-code issue (go:S107, 9 params) in server/internal/httpapi/admin_pools.go by introducing an AdminPoolsRegistryDeps struct for NewAdminPoolsWithRegistry, per the ticket.

1. Read the ticket fully and follow its Plan + Checklist EXACTLY:
   .scratch/sonar-server-new-code/issues/11-admin-pools-registry-deps.md

2. Implement: add AdminPoolsRegistryDeps struct (9 fields: Auth *Auth, Clients cluster.ClientProvider, Source inventory.LookupSource, Projection *inventory.Projection, Writer cluster.Writer, Audit vm.AuditRecorder, Refresher vm.IndexRefresher, Store *store.Store, Log *slog.Logger) documented, and change NewAdminPoolsWithRegistry(deps AdminPoolsRegistryDeps) *AdminPools to delegate to NewAdminPools(AdminPoolsDeps{...}) then set clients/source. Update EVERY NewAdminPoolsWithRegistry( call site — grep the WHOLE repo to find all (currently grep found only the definition; verify there is truly no external caller). NewAdminPools (struct form) and the AdminPools struct fields/receivers stay unchanged.

3. DO NOT add // NOSONAR — refactor to resolve the smell.

4. MANDATORY: DO NOT spawn parallel/background subagents. Edit files YOURSELF, sequentially.

5. Verify from the worktree root:
     make server-fmt && make server-vet && make server-lint && make server-test
   All four must pass. Fix until green.

6. When green, commit on this branch:
   git add -A && git commit -m "refactor(httpapi): AdminPoolsRegistryDeps struct for NewAdminPoolsWithRegistry (smell t11)"

7. Report concise summary: files changed, the S107 on admin_pools.go:57 resolved, test/lint result.
Do not touch files outside server/, and do not run make sonar*.
