You are working in an isolated git worktree of the PVMSS Go server repo (branch smell-t12), at /Users/jh/git/gh/wt-smell/t12.

TASK: Resolve 1 SonarQube new-code issue (go:S107, 8 params) in server/internal/httpapi/vm_bulk.go by introducing a VMBulkRegistryDeps struct for NewVMBulkWithRegistry, per the ticket.

1. Read the ticket fully and follow its Plan + Checklist EXACTLY:
   .scratch/sonar-server-new-code/issues/12-vm-bulk-registry-deps.md

2. Implement: add VMBulkRegistryDeps struct (8 fields: Registry *inventory.Registry, Projection *inventory.Projection, Auth *Auth, Writer cluster.Writer, Store *store.Store, Refresher vm.IndexRefresher, Log *slog.Logger, Clients cluster.ClientProvider) documented, and change NewVMBulkWithRegistry(deps VMBulkRegistryDeps) *VMBulk to build the VMBulk struct fields from deps. Update the call site at server/internal/httpapi/vm_bulk_test.go:199 to the struct form, and grep the WHOLE repo to confirm no other NewVMBulkWithRegistry( caller. VMBulk struct fields and ServeHTTP stay unchanged.

3. DO NOT add // NOSONAR — refactor to resolve the smell.

4. MANDATORY: DO NOT spawn parallel/background subagents. Edit files YOURSELF, sequentially.

5. Verify from the worktree root:
     make server-fmt && make server-vet && make server-lint && make server-test
   All four must pass. Fix until green.

6. When green, commit on this branch:
   git add -A && git commit -m "refactor(httpapi): VMBulkRegistryDeps struct for NewVMBulkWithRegistry (smell t12)"

7. Report concise summary: files changed, the S107 on vm_bulk.go:93 resolved, test/lint result.
Do not touch files outside server/, and do not run make sonar*.
